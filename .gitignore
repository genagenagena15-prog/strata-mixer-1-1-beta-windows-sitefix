# dependencies
node_modules/

# build output (regenerated on every build)
dist/
bin/
release/
release-installer/
release-mac/
installer-output/

# local-only tooling / caches
.wcs-tmp/
*.log

# OS junk
.DS_Store
Thumbs.db

# test scratch (this session)
_subtest/
NUL
NUL.css

# baked Groq API key (generated at build time from env / CI secret — never commit)
electron/groq-key.json
.claude/launch.json
